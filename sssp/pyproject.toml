[project]
name = "sssp"
version = "0.1.0"
description = "Distributed SSSP (Dijkstra/Bellman-Ford) benchmark using Burst Communication Middleware on OpenWhisk"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
